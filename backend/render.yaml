services:
  - type: web
    name: conversor-backend
    runtime: python
    rootDir: backend
    buildCommand: apt-get install -y pandoc && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT